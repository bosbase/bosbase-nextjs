export default function Feature3({ section }: { section: any }) {
  if (!section) return null;
  
  return (
    <section className="feature3">
      {/* Feature3 section content */}
    </section>
  );
}


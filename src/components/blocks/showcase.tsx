export default function Showcase({ section }: { section: any }) {
  if (!section) return null;
  
  return (
    <section className="showcase">
      {/* Showcase section content */}
    </section>
  );
}


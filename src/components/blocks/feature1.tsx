export default function Feature1({ section }: { section: any }) {
  if (!section) return null;
  
  return (
    <section className="feature1">
      {/* Feature1 section content */}
    </section>
  );
}


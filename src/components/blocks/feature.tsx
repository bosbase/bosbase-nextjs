export default function Feature({ section }: { section: any }) {
  if (!section) return null;
  
  return (
    <section className="feature">
      {/* Feature section content */}
    </section>
  );
}


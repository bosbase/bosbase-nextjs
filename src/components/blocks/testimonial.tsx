export default function Testimonial({ section }: { section: any }) {
  if (!section) return null;
  
  return (
    <section className="testimonial">
      {/* Testimonial section content */}
    </section>
  );
}


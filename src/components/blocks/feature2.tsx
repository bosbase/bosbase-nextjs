export default function Feature2({ section }: { section: any }) {
  if (!section) return null;
  
  return (
    <section className="feature2">
      {/* Feature2 section content */}
    </section>
  );
}


export default function ConsoleTable({ columns, data }: { columns?: any[]; data?: any[] }) {
  if (!columns || !data) return null;
  
  return (
    <div className="console-table">
      {/* Console table content */}
    </div>
  );
}

